# Optional defaults for the local UI. Credentials are read server-side from the active Google Cloud CLI login;
# do not put service account JSON or access tokens here.
NEXT_PUBLIC_DEFAULT_PROJECT_ID=
NEXT_PUBLIC_DEFAULT_DATASET=
NEXT_PUBLIC_DEFAULT_TABLE=
